class MinhaClasse {
    int soma(int a, int b) {
        return a + b;
    }
    
    void main() {
        int x = 10;
        int y = 20;
        int resultado = soma(x, y);
        
        if (resultado > 0) {
            float z = 3.14;
        }
        
        int[] vetor = new int[10];
        vetor[0] = 5;  // Corrigido: atribuição completa em uma linha
    }
}